<!-- The container is evidence-wide, but the text inside is capped at the
     reading measure by .prose-custom in global.css. That lets a screenshot or
     diagram break out to the full evidence tier while paragraphs stay at
     roughly 70 characters. -->
<div class="prose-custom prose prose-invert max-w-evidence">
  <slot />
</div>
