# Normalize line endings: store LF in the repo, let editors do what they like.
* text=auto eol=lf
*.png binary
*.jpg binary
*.woff2 binary
# vCard 3.0 requires CRLF line endings - never normalize.
*.vcf -text
